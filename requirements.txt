# (19-A) TECHO DE VERSION MAYOR — por que existe este archivo asi.
#
# `supervisor.py` corre `pip install -r requirements.txt` despues de cada
# `git reset --hard`, y vigila GitHub cada 5 min. Con `>=` sin techo, el
# dia que un paquete publica una version mayor la PC la instala SOLA, sin
# que nadie haya tocado una linea de codigo. `python-telegram-bot` rompe
# compatibilidad entre mayores con regularidad (20→21→22 lo hicieron), y
# el supervisor NO comprueba que el bot arranque ni sabe volver atras: el
# resultado seria un bucle de reinicios hasta intervencion manual.
#
# Ya paso una vez sin que nadie lo notara: el requisito decia `>=21.0` y
# la PC acabo con python-telegram-bot 22.8 (medido el 30/8/2026 en el
# venv). Funciono por suerte, no por diseño.
#
# El techo es la SIGUIENTE mayor, no una version exacta: asi se siguen
# recogiendo parches y correcciones de seguridad, pero un salto que puede
# romper la API exige una decision humana (subir el numero aqui despues
# de pasar la suite). Los minimos son los de siempre; los techos NO
# fuerzan ninguna instalacion ni degradacion en el PC, cuyas versiones
# actuales quedan todas dentro del rango.
#
# Versiones verificadas con la suite completa (582 pruebas verdes) el
# 30/8/2026: requests 2.34.2 · python-telegram-bot 22.8 · flask 3.1.3 ·
# psycopg2-binary 2.9.12 · pillow 12.3.0 · websocket-client 1.9.0

requests>=2.31,<3
python-telegram-bot[job-queue]>=21.0,<23
flask>=3.0,<4
psycopg2-binary>=2.9,<3
pillow>=10.0,<13
websocket-client>=1.7,<2
